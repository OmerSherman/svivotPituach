# dependencies
node_modules/

# production build
client/build/

# logs
*.log
npm-debug.log*

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/

# Backend env (now untracked; copy backend/.env.example to get started)
backend/.env

# NOTE: frontend/.env IS still committed on purpose for this assignment.
# It contains only a local API URL (mock data, no real secrets),
# so the frontend runs with zero configuration. Do NOT add frontend/.env here.

# personal planning docs, not part of the deliverable
PROJECT_OVERVIEW.md
SPEC-AI-TRIP-PLANNER.md
