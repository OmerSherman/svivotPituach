# dependencies
node_modules/

# production build
frontend/build/
client/build/

# logs
*.log
npm-debug.log*

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
backend/.idea/

# Backend env (copy backend/.env.example to get started)
backend/.env

# Legacy Server folder — do not commit secrets
Server/.env
Server/

# NOTE: frontend/.env IS still committed on purpose for this assignment.
# It contains only local API URLs (no secrets),
# so the frontend runs with zero configuration. Do NOT add frontend/.env here.

# personal planning docs, not part of the deliverable
PROJECT_OVERVIEW.md
SPEC-AI-TRIP-PLANNER.md

# standalone Wikivoyage scraper, not part of the app submission for now
backend/src/scraper/
