# dependencies
node_modules/

# production build
client/build/

# logs
*.log
npm-debug.log*

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/

# Server env (now untracked; copy Server/.env.example to get started)
Server/.env

# NOTE: client/.env IS still committed on purpose for this assignment.
# It contains only a local API URL (mock data, no real secrets),
# so the frontend runs with zero configuration. Do NOT add client/.env here.

# personal planning docs, not part of the deliverable
PROJECT_OVERVIEW.md
SPEC-AI-TRIP-PLANNER.md
